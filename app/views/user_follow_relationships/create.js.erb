$("#follow_user_<%= @user.id %>").html("<%= escape_javascript(render partial: 'users/unfollow_user', locals: { :user => @user } ) %>");
<% @boxes.each do | box | %>
$("#follow_box_<%= box.id %>").html("<%= escape_javascript(render('boxes/unfollow_box', box: box )) %>");
<% end %>

$("li#follower-pinbar strong").text(parseInt($("li#follower-pinbar strong").text())+ 1);


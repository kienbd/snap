<% unless current_user?(user) %>
  <% if current_user.following?(user) %>
    <%= render partial: 'users/unfollow_user', locals: { user: user } %>
  <% else %>
    <%= render partial: 'users/follow_user', locals: { user: user } %>
  <% end %>
<% end %>

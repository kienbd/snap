$('section.content').hide().html("<%=j render :partial => 'shared/photos_paginate' %>").fadeIn("slow");
$('#profile .FixedContainer a.toggle').removeClass('selected');
window.history.pushState('photosOfUser<%=@user.id%>','photos','<%= photos_user_path(@user.id) %>');
$('[href="' + window.location.pathname + '"]').addClass('selected');

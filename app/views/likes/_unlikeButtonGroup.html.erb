<%= form_for(current_user.likes.find_by_photo_id(photo.id), html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unlike", class: "btn btn-large firstChild" %>
<% end %>
<%= button_to photo.likes.count, '#', remote: true, class: "btn btn-large lastChild" %>

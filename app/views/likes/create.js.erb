$("#photo_<%=@photo.id%> .photo-like-btn").html("<%=j render partial: 'likes/unlike', locals:{photo: @photo}%> ");
$el = $('.left_content .actions .repinLike .groupButton.like');
if ($el.length != 0)
  $el.html("<%=j render partial: 'likes/unlikeButtonGroup', :locals => { photo: @photo }%>");
